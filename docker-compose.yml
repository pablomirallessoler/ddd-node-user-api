version: '3.7'

services:
  mongo:
    image: mongo:4.2
    restart: always
    container_name: ddd-node-mongo
    ports:
      - 27017:27017
    volumes:
      - ddd-node-mongo:/data/db

volumes:
  ddd-node-mongo: